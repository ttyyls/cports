diff --git a/Makefile b/Makefile
index 43c4227a..98e1ba8a 100644
--- a/Makefile
+++ b/Makefile
@@ -52,7 +52,7 @@ $(BINOUT)/haredoc: $(BINOUT)/hare
 	@mkdir -p $(BINOUT)
 	@printf 'HARE\t%s\n' "$@"
 	@env $(HARE_BUILD_ENV) \
-		$(BINOUT)/hare build $(HARE_DEFINES) -o $(BINOUT)/haredoc ./cmd/haredoc
+		$(BINOUT)/hare build $(HARE_DEFINES) -a$(ARCH) -o $(BINOUT)/haredoc ./cmd/haredoc
 
 docs/html: $(BINOUT)/haredoc
 	mkdir -p docs/html
